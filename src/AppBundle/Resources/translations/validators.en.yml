person:
    age:
        min:  'Minimum age: {{ limit }}'
        max: 'Maximum age: {{ limit }}'
        invalid: 'Invalid value.' 

    height:
        min: 'Minimum height: {{ limit }}'
        max: 'Maximum height: {{ limit }}'
        invalid: 'Invalid value.'

    email:
        invalid: 'Email invalid. {{ value }}'

    phone: 
        invalid: 'Phone number must be between 9 and 12 digits.'

    gender:
        invalid: 'Wrong gender.'

    descends:
        min: 'Minimum number: {{ limit }}'
        max: 'Maximum number: {{ limit }}'
        invalid: 'Invalid value'

    preferred_languaje:
        invalid: 'Wrong language'

    URL:
        invalid: 'Invalid WEB'

    card_number:
        invalid: 'Credit card invalid'

    iban:
        invalid: 'Wrong (IBAN).'